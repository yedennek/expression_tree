Refactoring exercise of an expression tree implementation

## Setup

You'll need to have nodejs installed.

To install dependencies:

`yarn install`

To run the file and assertions:

`node lib/tree.js`
